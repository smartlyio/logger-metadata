name: Release and Publish to rubygems

on:
  push:
    branches: [master]

jobs:
  @import ./jobs/test

  @import smartlyio/jobs/ruby/publish (publish_needs: ["test"], ruby_publish_to_rubygems: true, ruby_publish_token_secret_name: "GEM_HOST_API_KEY")
